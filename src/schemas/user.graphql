# import Exercise from 'exercise.graphql'

enum WeightUnit {
  KG
  LBS
}

type User {
  username: String!
  exercises: [Exercise]
  settings: UserSettings
}

type UserSettings {
  unit: WeightUnit!
}
